name: Delbites
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  uuid: ^3.0.7  # You can use the latest version available
  webview_flutter: ^4.2.2
  cupertino_icons: ^1.0.8
  sqflite: ^2.4.1
  curved_navigation_bar: ^1.0.6
  http: ^1.0.3
  intl: ^0.18.1
  dio: ^5.8.0+1
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  cloud_firestore: ^5.6.6
  flutter_launcher_icons: ^0.13.1
  shared_preferences: ^2.2.2
  url_launcher: ^6.1.10
  firebase_messaging: ^15.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/logo1.png"

flutter:
  uses-material-design: true
  assets:
    - assets/icon/logo1.png
